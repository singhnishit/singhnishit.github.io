---
import { ViewTransitions } from 'astro:transitions';
import ModelAnswer from '../components/ModelAnswer.astro';
interface Props {
  title: string;
  subtitle?: string;
  pageTitle?: string;
  fitContent?: boolean;
}
const { title, subtitle, pageTitle, fitContent = false } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{pageTitle ?? title} — Nishit Singh</title>
    <link rel="stylesheet" href={import.meta.env.BASE_URL + "style.css"}>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
    <link rel="icon" type="image/x-icon" href={import.meta.env.BASE_URL + "favicon.gif"} />
    <ViewTransitions />
</head>
<body>
    <ModelAnswer />
    <div class="page-wrapper">
        <header>
            <div class="header-left">
                <h1>{title}{subtitle && <span class="pronouns"> {subtitle}</span>}</h1>
            </div>
            <div class="header-right">
                <a href={import.meta.env.BASE_URL}>home</a> / <a href={import.meta.env.BASE_URL + "blog"}>blog</a> / <a href="#">cv</a> / <a href="#">github</a>
            </div>
        </header>

        <div class:list={["container", { "container-fit-content": fitContent }]}>
            <slot />
        </div>
    </div>
</body>
</html>
